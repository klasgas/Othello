
using UnityEngine;
using System.Collections;
using OthelloLogic;


public class PositionHandler : MonoBehaviour {
/*
	private Board _currentBoard;
	//public GameObject discPrefab;
	public static float tileSize = 1.1f;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public void SetPosition2(Board board)
	{
		if (_currentBoard == null) {
			_currentBoard = board;

			for(int x = 0; x < 8; x++)
			{
				for(int y = 0; y < 8; y++)
				{
					Square square = _currentBoard._squares[x][y];
					Square.SquareValue value = square.GetValue();
					if(value != Square.SquareValue.Empty)
					{
						Debug.Log (string.Format("x={0} y={1} value={2}", x, y, square.GetValue()));
						DiscPlacer.PlaceDisc(x, y, value);
						//PlaceDisc(x, y, value);
					}
				}
			}
		}
	}
	*/
}
